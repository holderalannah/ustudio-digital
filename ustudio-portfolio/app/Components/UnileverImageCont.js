import Image from 'next/image';

export default function UnileverImageContainer({imgSrc, altText}){
    return(
        <div className='overflow-hidden relative z-20'>
            
        </div>
    )
}